<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" /> <!-- Цвет фона, например, прозрачный -->
    <stroke
        android:width="2dp"
        android:color="@android:color/white" /> <!-- Цвет обводки, если нужно -->
</shape>